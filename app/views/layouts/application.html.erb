<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" />
  </head>

  <body>
    <header class="p-3 text-bg-dark">
      <div class="container">
        <div class="d-flex w-100 flex-wrap align-items-center justify-content-end">
          <% if user_signed_in? %>
            <div class="text-end">
              <%= link_to 'Logout', destroy_user_session_path, class: "btn btn-warning", data: { turbo_method: :delete } %>
            </div>
          <% else %>
            <div class="text-end">
              <%= link_to 'Login', new_user_session_path, class: "btn btn-outline-light me-2" %>
              <%= link_to 'Sign-up', new_user_registration_path, class: "btn btn-warning" %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <div class="container-fluid">
    #  <p class="notice"><%= notice %></p>
    #  <p class="alert"><%= alert %></p>

      <%= yield %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
